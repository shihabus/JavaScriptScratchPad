# Types in JS

## Primitive Types

These are the basic noes of which all other types are build on

- Number
- undefined
- null, but ```typeof null``` is ```Object``` 
- String
- Symbol
- Boolean
- bigInt, new one

## Composite Types

- Function
- Object 
- Array, ```typeof Array``` is ```Object```

### NaN **Not A Number**
If the result of a Mathematical operation is not a number then the result will have a type of **NaN**

```
var name='Shihab';
var result=name/2;
console.log(typeof result); //NaN

Number.isNaN(name);  //false
Number.isNaN(result); //true
```

## Coersion

```
number+number=number
number+string=string
string+number=string
string+string=string
```

### Equality

```==``` coerced comparsion
```===``` non-coerced comparison

```null==undefined``` true
```null===undefined``` false

```3.04=='3.040000'``` true
```3.04==='3.040000'``` flase

# Scope

https://playcode.io/520405
 
Scope is wehere to look for things.


```
var teacher='Shihab';

function mutateInfo(){
  teacher='Abbe';
  tech='React';
  level='Inter';
 }
 
console.log(teacher); //Shihab
console.log(tech); //undefined
console.log(level); //undefined
 
mutateInfo();
console.log(teacher); //Shihab
console.log(tech); //React
console.log(level); //undefined
 
 ````
 
 variable that are never declared (undeclared), and decalred but uninitialized(undefined) are
 both ```undefined``` in JS.
 
 
 # Closure
 
 A function remembering a variable defined outside it's execution context.
 
 https://playcode.io/520521
 












